# Skopiuj na serwer jako: domains/frontlinepulse.eu/frontline/.env
# (DirectAdmin → menedżer plików → nowy plik .env)

NODE_ENV=production
DATABASE_URL=postgresql://USER:PASS@ep-....neon.tech/neondb?sslmode=require
CRON_SECRET=wygeneruj_openssl_rand_hex_24
