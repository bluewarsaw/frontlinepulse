# /etc/nginx/sites-available/frontline
# Reverse proxy: domena -> aplikacja Next.js na porcie 3000.
# Po zapisaniu: ln -s do sites-enabled, nginx -t, systemctl reload nginx,
# a następnie certbot --nginx -d twojadomena.pl (darmowy SSL Let's Encrypt).

server {
    listen 80;
    server_name twojadomena.pl;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # statyki Next.js — długi cache
    location /_next/static/ {
        proxy_pass http://127.0.0.1:3000;
        proxy_cache_valid 200 7d;
        add_header Cache-Control "public, max-age=31536000, immutable";
    }
}
